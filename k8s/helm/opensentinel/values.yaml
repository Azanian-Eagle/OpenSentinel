replicaCount: 3

image:
  repository: ghcr.io/azanian-eagle/opensentinel
  pullPolicy: Always
  tag: "latest"

service:
  type: ClusterIP
  port: 8080

resources: {}
  # limits:
  #   cpu: 100m
  #   memory: 128Mi
  # requests:
  #   cpu: 100m
  #   memory: 128Mi

secretName: opensentinel-secrets
